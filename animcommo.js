const spotifyAccountsBaseUri = "https://accounts.spotify.com";
const tokenEndpoint = `${spotifyAccountsBaseUri}/api/token`;

console.log(tokenEndpoint);  // Output: https://accounts.spotify.com/api/token
